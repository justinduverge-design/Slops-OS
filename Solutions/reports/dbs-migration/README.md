# DBS Migration Reports

This folder contains historical DBS migration reports.

Many files here intentionally mention the former `ssffmvp` app path, old `Corvus/` product path, and prior migration decisions. Those references are preserved as audit history and are not current routing instructions.

Current layer routing is:

```text
Layer 0: SLOPS/
Layer 1: SLOPS/slops-saloon/
Layer 2: SLOPS/slops-saloon/corvus/
```

Use the current root `context.md`, `DBS_INDEX.md`, `README.md`, `AGENTS.md`, and `CLAUDE.md` files before relying on any historical report here.
