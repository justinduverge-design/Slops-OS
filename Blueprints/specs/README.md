# ssffmvp Specs

This folder remains at the repo root because repo docs, prompts, skills, and handoffs reference `specs/` paths.

Use it for active app/repo specs and spec-kit style project material.

Corvus product-specific copies may also live in `Corvus/Blueprints/specs/`.
