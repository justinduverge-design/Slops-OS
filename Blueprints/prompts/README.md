# Prompt Blueprints

Use this folder for reusable prompt blueprints after they are no longer required at root paths.

Root SLOPS prompt blueprints live here.

The active app prompt docs were moved to `ssffmvp\Blueprints\prompts\`. Some source and eval references may still expect the old `ssffmvp\prompts\` path and need a separate implementation review before changing runtime behavior.
