# Prior Use Review — slops-mobile-smoke

This file accumulates one-liners after each routine run so the skill stays honest about real
performance over time.

Format:
```
<YYYY-MM-DD> — <target URL> — P0:<n> P1:<n> P2:<n> — <one-line note about false positives or drift>
```

## Log

_(empty — proposal stage, not yet implemented)_
