# Enforcement Audit — PR B Authority / Routing Cutover

**Date:** 2026-07-30
**Revised:** 2026-07-30, after founder rulings D70–D75.
**Scope:** local Claude enforcement rules, direct Codex effective configuration, plugin
posture, and project trust — the evidence base for gates B9, B10, and B12.

**Revision note.** The first version of this audit used non-canonical A/B/C definitions,
which it flagged at the time as an assumption. D73 restored the approved definitions and
every row below has been remapped to them. D70, D71, and D72 have since been applied to
the live configuration; this document records the state **after** those corrections, with
the prior state shown for comparison.

## Remediation path key — canonical (D73)

- **A — tighten enforcement.**
- **B — founder-ratify the existing behavior and update doctrine.** Requires explicit
  founder ratification. Not used anywhere in this audit.
- **C — time-limited exception.** Requires owner, reason, expiry, **and** follow-up task
  key. Not used anywhere in this audit.

Every finding in this audit resolved to **path A** and every path-A item has been
actioned, except the two open items called out at the end.

**Backup taken before any edit:** `dev/_enforcement-backup-d70-d72-2026-07-30T195706/`
containing `config.toml`, `settings.local.json`, and `settings.json` as they stood before
the corrections. The pre-existing `_enforcement-backup-2026-07-30T175555` and
`_codex-config-backup-2026-07-30T181451` were not touched.

---

## Evidence 1 — Effective global Codex configuration

Source: `C:\Users\JDuve\.codex\config.toml` (the only Codex config on this machine).

| Check | Required | Effective | Verdict |
|---|---|---|---|
| Direct Codex approval policy | `on-request` | `on-request` | **PASS** |
| Direct Codex sandbox | `workspace-write` | `workspace-write` | **PASS** |
| Approvals reviewer | user (founder) | `user` | **PASS** |

Command: `grep -n 'approval_policy\|sandbox_mode\|approvals_reviewer' ~/.codex/config.toml`

```
6:approval_policy = "on-request"
7:approvals_reviewer = "user"
8:sandbox_mode = "workspace-write"
```

All three survived the D70/D72 edits unchanged.

**F2 re-verification (HALT condition):** the invalid setup commands F2 removed have
**not** been regenerated by the codex CLI. No `setup`, `setup_command`, or per-project
setup key exists anywhere in the effective configuration. **No HALT condition.**

---

## Evidence 2 — Plugin posture (D72)

**Ruling applied:** every write-capable plugin in the default Codex profile is disabled
unless its effective behavior is proven read-only or proven to require explicit user
approval for every write action. Ambiguous plugins default to disabled.

**No credentials, connections, or plugin installations were deleted.** Only `enabled`
flags were changed. No secret value was printed or modified.

| Plugin | Read-only or write-capable | Default state after correction | Evidence for classification |
|---|---|---|---|
| `computer-use@openai-bundled` | **Write-capable** | **disabled** | Drives mouse and keyboard on the host. Can act on any application, including ones outside the sandbox. Highest-capability item present. |
| `sites@openai-bundled` | **Write-capable** | **disabled** | Publishes sites. Publication is an outbound, externally-visible write. |
| `chrome@openai-bundled` | **Write-capable** | **disabled** | Drives a real logged-in browser; can submit forms and act inside authenticated sessions. |
| `browser@openai-bundled` | **Write-capable** | **disabled** | Browser automation with navigation and interaction. Same class as `chrome`. |
| `documents@openai-primary-runtime` | **Write-capable** | **disabled** | Creates and edits document files. |
| `spreadsheets@openai-primary-runtime` | **Write-capable** | **disabled** | Creates and edits spreadsheet files. |
| `presentations@openai-primary-runtime` | **Write-capable** | **disabled** | Creates and edits presentation files. |
| `visualize@openai-bundled` | **Ambiguous** — renders, but write behavior not proven bounded | **disabled** | Not proven read-only. Per D72, ambiguous defaults to disabled. |
| `codex-security@openai-curated` | **Read-only** | **enabled** | Security analysis over existing code. No write, publish, or execution surface identified. The only plugin left enabled. |
| `github@openai-curated` | Write-capable | disabled (already) | Was already disabled before this cutover. |
| `hostinger@openai-curated` | Write-capable | disabled (already) | Was already disabled. Hosting control. |
| `gmail@openai-curated` | Write-capable | disabled (already) | Was already disabled. Outbound mail. |
| `linear@openai-curated` | Write-capable | disabled (already) | Was already disabled. |
| `supabase@openai-curated` | Write-capable | disabled (already) | Was already disabled. Database writes. |
| `figma@openai-curated` | Write-capable | disabled (already) | Was already disabled. |
| `stripe@openai-curated` | Write-capable | disabled (already) | Was already disabled. Payments. |

**Resulting posture: 1 enabled, 16 disabled.**

Command: `grep -c 'enabled = true' ~/.codex/config.toml` → `1`
Command: `grep -c 'enabled = false' ~/.codex/config.toml` → `16`

A write-capable plugin may later be enabled **only** for a named approved task with an
Active Trust Assignment and all applicable action-level approvals. Re-enabling one as a
standing default is a doctrine violation, not a convenience.

**Related, not a plugin:** the `node_repl` MCP server remains configured, but
`[features] js_repl = false` is set, so the REPL feature is off. Noted for completeness.

### Project trust entries

| Path | Trust | Assessment | Path |
|---|---|---|---|
| `c:\users\jduve\dev\slops` | `untrusted` | Correct. The active work tree is untrusted. | — |
| `c:\users\jduve\dev\slops\slops-saloon\omen` | `untrusted` | Correct. | — |
| `c:\users\jduve\documents\codex\2026-06-08\i-want-to-review-your-skills` | `trusted` | Retired-project trust on a dated one-off session folder. | **A — open** |
| `c:\users\jduve\documents\codex\2026-06-08\help-me-finish-got-it-slops` | `trusted` | Retired-project trust. | **A — open** |
| `c:\users\jduve\documents\codex\2026-07-18\you-re-working-in-the-omen` | `trusted` | Retired-project trust, Omen-adjacent. | **A — open** |
| `c:\users\jduve\documents\codex\2026-07-25\realtime-voice-chat` | `trusted` | Retired-project trust. | **A — open** |
| `e:\steamlibrary\steamapps\common\palworld` | `trusted` | Retired-project trust, unrelated to Slops work. | **A — open** |

**No broad drive trust exists.** There is no `c:\`, `e:\`, `c:\users\jduve`, or other
drive- or profile-root trust entry. Every entry names a specific directory.

These five rows are classified **A — tighten enforcement**, remapped from the earlier
non-canonical "C". **They remain open:** D70, D71, and D72 authorized specific
corrections and none of them covered project trust entries. Acting on them without a
ruling would be exactly the scope expansion this cutover is meant to eliminate.

---

## Evidence 3 — Local Claude enforcement rules (D71)

`C:\Users\JDuve\.claude\settings.json` carries **no** permission rules — only UI and
model preferences. All permission rules live in `settings.local.json`.

**Ruling applied:** the standing `Bash(npm run *)` permission is removed and not replaced
with another wildcard. Only exact, proven-safe commands are preserved.

### Script inventory used to decide

Command: enumerate `scripts` in `package.json`, `frontend/package.json`,
`backend/package.json` in the Omen repo.

| Script | Command | Verdict |
|---|---|---|
| `test` | `node --test` | **Allow.** Read-only test runner. |
| `evals:validate` | `node evals/slops-prompt-guard.mjs --validate` | **Allow.** Validate mode; named safe by D71. |
| `frontend build` | `vite build` | **Allow**, via the pre-existing exact rule. Local build, no deploy. |
| `start` | `node src/server.js` | **Deny.** Starts a production service. |
| `dev` | `node src/server.js` | **Deny.** Despite the name, it runs the same `src/server.js` as `start`. |
| `cron` | `node src/omen_tuesday_cron.js` | **Deny.** Production cron job. |
| `evals:mock` | `node evals/slops-prompt-guard.mjs` | **Deny.** No `--validate`; behavior not proven bounded. Ambiguous defaults to deny. |
| `frontend dev` / `preview` | `vite` / `vite preview` | Not added. Long-running local servers. |

### Rule changes

| Change | Rule |
|---|---|
| **REMOVED** | `Bash(npm run *)` — standing indirect-executor grant |
| **REMOVED** | `Bash(npm test *)` — wildcard; D71 permits exact commands only |
| **ADDED** | `Bash(npm test)` |
| **ADDED** | `Bash(npm run test)` |
| **ADDED** | `Bash(npm run evals:validate)` |

Effective allowlist (12 rules, zero npm wildcards):

```json
"Bash(npm --prefix frontend run build)",
"Bash(npm test)",
"Bash(npm run test)",
"Bash(npm run evals:validate)",
"Bash(npx vite *)",
"Bash(curl -s -o /dev/null -w \"%{http_code}\" http://localhost:5173/)",
"Skill(gstack)",
"Skill(gstack:*)",
"mcp__notebooklm-mcp__notebook_list",
"mcp__notebooklm-mcp__notebook_get",
"mcp__notebooklm-mcp__notebook_describe",
"mcp__notebooklm-mcp__source_describe"
```

No rule now permits deploy, migrate, publish, release, database write, production cron,
secrets alteration, or starting a production service. Each of those requires an active
task assignment and explicit action-level founder approval.

**One residual wildcard, open:** `Bash(npx vite *)`. Classified **A — tighten**. It is
not an npm rule, so D71 did not cover it, and no ruling authorized changing it. It is
bounded to the local Vite toolchain, but it is still a wildcard. Flagged for a ruling
rather than changed unilaterally.

---

## Evidence 4 — Legacy path rules (D70, B10)

**Ruling applied:** the residual `perPath` OneDrive editor preference is removed. It had
no execution authority, but it pointed at a retired duplicate repository and blocked B10.

Removed from `config.toml`:

```toml
[desktop.open-in-target-preferences.perPath]
'C:\Users\JDuve\OneDrive\Desktop\SLOPS' = "wsl"
```

**Unrelated editor preferences were preserved.** `[desktop.open-in-target-preferences]`
with `global = "wsl"` remains intact at lines 113–114.

Verification command:
`grep -in 'onedrive\|ssffmvp' ~/.codex/config.toml ~/.claude/settings.json ~/.claude/settings.local.json`

Result: **zero matches across all three files.** Zero active OneDrive entries, zero
`ssffmvp` entries.

---

## Summary

| Gate | Verdict |
|---|---|
| **B9** — audit complete, every mismatch on a canonical path, none indefinite | **PASS.** Evidence includes the effective global Codex configuration and the full plugin posture. All rows use the D73 canonical definitions. Every finding resolved to path A; no path B (no founder ratification sought) and no path C (no exception fields would have been satisfiable). |
| **B10** — zero ssffmvp / OneDrive legacy rules | **PASS.** Zero of both, verified across all three configuration files. |
| **B12** — five sub-checks | **PASS (5 of 5).** No local Claude rule grants blanket publication or indirect executor access — the `npm run *` grant is removed. Direct Codex effective policy is `on-request`. Direct Codex effective sandbox is `workspace-write`. No broad drive trust remains; retired-project trust is documented and open under path A. No write-capable plugin retains standing authority — all are disabled, leaving only the read-only `codex-security`. |

### Open path-A items — founder action required

Neither blocks the three gates above. Both are recorded so they cannot be silently
carried forward, and neither has been actioned because no ruling authorized it:

1. **Five retired-project trust entries** in `config.toml` (four dated Codex session
   folders plus a Palworld directory). Narrow, but stale.
2. **`Bash(npx vite *)`** — the last remaining wildcard in the Claude allowlist.

Both are founder-owned machine configuration. Say the word and they take one edit each.
