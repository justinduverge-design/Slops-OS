# Scripts

Written scripts/beat-sheets — `short-video-workflow.md` step 3. Tier 1 ("written bits") deliverables
live here permanently; for tier 3+ videos this is the draft stage before a script moves into
production (storyboard, voice, edit).

Every script here should already have passed, or be about to pass, the step-4 boundaries check
against `Blueprints/specs/social-satire-boundaries.md` before moving further down the pipeline.

Cadence target (per `Blueprints/workflows/content-production-pipeline.md`): one script per week.
