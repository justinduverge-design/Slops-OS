# Slops Saloon resolver

This L1 resolver specializes the canonical L0 rules in `../RESOLVER.md`.

## Stay at L1 when

- The fact governs Slops Saloon as a division.
- The work concerns cross-product brand custody, content, marketing, sports/music/arts strategy, or a future product slot.

## Route away when

- Reusable operating doctrine or the Valor Brain contract itself → L0.
- Omen product facts, source, tests, delivery, or operations → L2 in `omen/`.

## Valor Brain

L1 uses the canonical L0 profile and validator; it does not carry a duplicate schema.

From the L0 repository root:

```powershell
node Blueprints/tools/valor-brain/validate.mjs
```

Physical placement still follows L1 DBS purpose. If a page does not fit an existing L1 route, stop rather than creating a new metadata-driven folder.
