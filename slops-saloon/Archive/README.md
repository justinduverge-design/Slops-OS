# Slops Saloon Archive

This folder preserves superseded, parked, or historical Layer 1 division material.

Archive is not active truth.

## Index

- `Archive/planning-pipeline/2026-07-29-pre-status-model/` — L1 `Direction/` wrappers and the retired second queue (`TODO.md`), archived when the planning pipeline moved to the status model. See its `MANIFEST.md`. Historical discovery only; not active authority.

Do not archive, inspect, move, or edit secrets, `.env`, key-like files, active source, SQL, package files, deploy config, `.git`, `node_modules`, or production material from this layer without explicit Justin approval.
